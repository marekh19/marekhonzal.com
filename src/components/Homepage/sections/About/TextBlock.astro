---
import { cn } from '@/lib/utils/cn'

type Props = {
  className?: string
}

const { className } = Astro.props
---

<p class={cn('leading-relaxed not-first-of-type:mt-5', 'lg:text-lg', className)}>
  <slot />
</p>
