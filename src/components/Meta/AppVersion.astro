---
import { getAppVersion } from '@/lib/utils/version'

const version = getAppVersion()
---

<!-- APP DEBUG INFO -->
<meta name="app:version" content={version} />
<meta name="app:build-date" content={new Date().toISOString()} />
