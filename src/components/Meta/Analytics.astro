---
import { ENV } from '@/config/env'

const config = {
  url: 'https://a7s.hnzl.dev/script.js',
  websiteId: ENV.PUBLIC_UMAMI_SITE_ID,
} as const
---

<script is:inline defer src={config.url} data-website-id={config.websiteId}></script>
