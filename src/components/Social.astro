---
type Props = {
  platform: string
  username: string
}

const { platform, username } = Astro.props
---

<a
  href={`https://www.${platform}.com/${username}`}
  rel="noopener noreferrer"
  target="_blank"
  class="px-2 py-4"
  >{platform}</a
>
